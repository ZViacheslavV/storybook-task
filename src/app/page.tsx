import Input from '@/components/Input/Input';
import css from './Home.module.css';

const Home = () => {
  return (
    <main>
      <div className={css.container}>
        <Input />
      </div>
    </main>
  );
};

export default Home;
